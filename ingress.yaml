---
apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "ansible"
  namespace: "aap-op"
spec:
  defaultBackend:
    service:
      name: "ansible-controller-web-svc"
      port:
        number: 8052
